import { notFound } from 'next/navigation';
import Link from 'next/link';
import { createClient } from '@/lib/supabase_server';
import RenterNavbar from '@/components/renter/renter-navbar';
import { statusLabel } from '@/lib/rentalStatus';
import type { RentalBooking } from '@/lib/types';

export default async function RentalDetailsPage({ params }: { params: Promise<{ bookingId: string }> }) {
  const supabase = await createClient();
  const { data: userData } = await supabase.auth.getUser();
  if (!userData.user) notFound();
  const { bookingId } = await params;
  const { data: booking, error } = await supabase.from('tbl_bookings').select('*, tbl_units(*)').eq('booking_id', bookingId).eq('user_id', userData.user.id).single();
  if (error || !booking) notFound();
  const rental = {
    ...booking,
    booking_status: booking.status ?? 'pending',
  } as RentalBooking;
  const unit = rental.tbl_units;
  const { data: profile } = await supabase.from('tbl_users').select('first_name, last_name').eq('user_id', userData.user.id).maybeSingle();

  return <div className="min-h-screen overflow-x-hidden bg-[#fbfdff] text-slate-900"><div className="pointer-events-none fixed inset-0 -z-10 overflow-hidden"><div className="absolute -right-40 -top-40 h-[620px] w-[620px] rounded-full bg-sky-200/35 blur-3xl" /><div className="absolute -left-56 top-[35%] h-[500px] w-[500px] rounded-full bg-blue-100/40 blur-3xl" /></div><RenterNavbar firstName={profile?.first_name ?? undefined} lastName={profile?.last_name ?? undefined} /><main className="mx-auto min-h-[calc(100vh-64px)] max-w-[1100px] px-6 pb-28 pt-10 lg:px-10"><Link href="/renter/my-rentals" className="text-sm font-semibold text-blue-600 hover:text-blue-700">← Back to my rentals</Link><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><section className="overflow-hidden rounded-[30px] border border-white/80 bg-white/85 shadow-[0_20px_70px_rgba(37,99,235,0.08)] backdrop-blur"><div className="flex h-64 items-center justify-center border-b border-slate-100 bg-gradient-to-br from-blue-50 to-sky-100"><span className="rounded-full border border-dashed border-blue-200 bg-white/80 px-4 py-2 text-xs font-semibold uppercase tracking-[0.16em] text-blue-400">Image coming soon</span></div><div className="space-y-5 p-7"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-blue-600">Rental details</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em] text-slate-950">{unit?.unit_name || `Rental #${rental.booking_id}`}</h1></div><p className="leading-7 text-slate-600">{unit?.description || 'Your rental details and booking schedule are shown here.'}</p><dl className="grid gap-4 border-t border-slate-100 pt-5 sm:grid-cols-2"><div><dt className="text-xs uppercase tracking-[0.14em] text-slate-500">Category</dt><dd className="mt-1 font-semibold text-slate-900">{unit?.category || 'Rental'}</dd></div><div><dt className="text-xs uppercase tracking-[0.14em] text-slate-500">Location</dt><dd className="mt-1 font-semibold text-slate-900">{String(unit?.location || 'Metro Manila')}</dd></div></dl></div></section><aside className="h-fit rounded-[30px] border border-white/80 bg-white/85 p-7 shadow-[0_20px_70px_rgba(37,99,235,0.08)] backdrop-blur"><div className="flex items-center justify-between gap-3"><h2 className="text-lg font-bold text-slate-900">Booking summary</h2><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{statusLabel(rental.status ?? 'pending')}</span></div><dl className="mt-6 space-y-4 text-sm"><div className="flex justify-between gap-4"><dt className="text-slate-500">Start date</dt><dd className="font-semibold text-slate-900">{rental.start_date}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">End date</dt><dd className="font-semibold text-slate-900">{rental.end_date}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Rental duration</dt><dd className="font-semibold text-slate-900">{rental.total_days || 0} day(s)</dd></div><div className="flex justify-between gap-4 border-t border-slate-100 pt-4"><dt className="text-slate-500">Total</dt><dd className="text-xl font-bold text-blue-600">₱{Number(rental.total_amount || 0).toLocaleString()}</dd></div></dl>{rental.notes ? <div className="mt-5 rounded-xl bg-slate-50 p-4 text-sm text-slate-600"><p className="font-semibold text-slate-800">Notes</p><p className="mt-1">{rental.notes}</p></div> : null}</aside></div></main></div>;
}